# 🛡️ RU Financial Monitoring Helper

![CI](https://github.com/ranas-mukminov/ru-fin-monitoring-helper/workflows/CI/badge.svg)
![License](https://img.shields.io/badge/license-Apache--2.0-blue.svg)
![Python](https://img.shields.io/badge/python-3.10%2B-blue.svg)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

## Описание

RU Financial Monitoring Helper — это комплексный стартовый набор для внедрения observability финансового мониторинга (FinMon/AML) в российских финтех-, платёжных и криптоплатформах. Проект предоставляет готовые к использованию YAML-схемы для логирования событий аутентификации, KYC, транзакций и проверки санкционных списков, а также преднастроенные пайплайны ELK/Loki, дашборды Grafana и детерминированные AI-помощники для генерации наборов событий и плейбуков соответствия. Предназначен для команд DevSecOps, этот инструментарий сфокусирован на технической реализации — что логировать, как создавать пайплайны и визуализировать события финмониторинга — не заменяя юридических или комплаенс-консультаций.

## Основные возможности

- **Схемы событий** — YAML-определения основных типов событий FinMon: аутентификация, жизненный цикл KYC, транзакции, AML-флаги, проверка санкций, управление кейсами
- **Шаблоны пайплайнов** — Готовые к использованию конфигурации Logstash и Loki для приёма и индексации финансовых событий
- **Дашборды Grafana** — Преднастроенные дашборды мониторинга для финансовых операций и отслеживания AML-активности
- **Заглушки интеграции с санкциями** — Клиентские интерфейсы для OpenSanctions и Sanctions Network (примеры без API-ключей)
- **CLI-инструментарий** — Валидация схем, генерация конфигураций пайплайнов и создание комплаенс-артефактов
- **AI-помощники** — Детерминированные предложения наборов событий и черновиков плейбуков на основе профилей продуктов (офлайн, без внешних API-вызовов по умолчанию)
- **Фокус на российский рынок** — Адаптировано под российскую регуляторную среду (115-ФЗ, правила финмониторинга) с сохранением vendor-neutral архитектуры
- **TDD-first подход** — Комплексное покрытие тестами с pytest, линтингом и сканированием безопасности

## Архитектура и компоненты

Проект следует модульной структуре:

```
┌─────────────────┐
│ CLI-команды     │ ← Валидация схем, генерация пайплайнов, AI-помощники
└────────┬────────┘
         │
    ┌────┴─────────────────────────────────┐
    │                                       │
┌───▼──────────┐                   ┌───────▼────────┐
│ Схемы событий│                   │ AI-помощники   │
│ (YAML)       │                   │ (офлайн)       │
└──────┬───────┘                   └────────────────┘
       │
       │ подаются в
       │
┌──────▼───────────────────────────────────┐
│ Пайплайны (ELK/Loki)                     │
│ ├─ Конфигурации Logstash                 │
│ └─ Экстракторы меток Loki                │
└──────┬───────────────────────────────────┘
       │
       │ визуализируются в
       │
┌──────▼───────────────────────────────────┐
│ Дашборды Grafana                         │
│ ├─ Мониторинг аутентификации/сессий      │
│ ├─ Объёмы транзакций                     │
│ └─ AML-флаги и подозрительная активность │
└──────────────────────────────────────────┘
```

Внешние интеграции (санкционные списки) абстрагированы через клиентские интерфейсы — продакшн-ключи API не включены.

## Требования

### Системные требования

- **ОС**: Linux (Ubuntu 20.04+, Debian 11+, RHEL 8+, Rocky Linux 8+)
- **Python**: 3.10 или выше
- **Память**: 2GB+ RAM для разработки; 8GB+ рекомендуется для ELK-стека
- **Диск**: 5GB+ для кодовой базы и логов; 50GB+ для данных Elasticsearch
- **Сеть**: Доступ в Интернет для загрузки зависимостей и Docker-образов

### Зависимости ПО

- `pip` и `virtualenv` (или `venv`)
- Docker и Docker Compose (для запуска примеров ELK/Loki/Grafana)
- Git

### Требования к доступу

- Обычный пользователь с доступом `sudo` (для операций с Docker)
- Root не требуется для CLI-инструментов

## Быстрый старт (TL;DR)

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/ranas-mukminov/ru-fin-monitoring-helper.git
cd ru-fin-monitoring-helper

# 2. Установите пакет
pip install -e .[dev]

# 3. Валидируйте схемы событий
ru-fin-helper validate-schema --file schemas/events/transaction_core.yaml

# 4. Сгенерируйте предложения событий для вашего продукта
ru-fin-helper suggest-events \
  --product examples/products/simple_psp_ru.yaml \
  --out /tmp/suggested_events.yaml

# 5. Запустите ELK-стек для тестирования
docker compose -f examples/elk/docker-compose.elk-example.yml up -d

# 6. Откройте Kibana по адресу http://localhost:5601
```

## Подробная установка

### Установка на Ubuntu / Debian

```bash
# Обновите индекс пакетов
sudo apt update

# Установите Python 3.10+ и pip
sudo apt install -y python3 python3-pip python3-venv git

# Клонируйте репозиторий
git clone https://github.com/ranas-mukminov/ru-fin-monitoring-helper.git
cd ru-fin-monitoring-helper

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите пакет с dev-зависимостями
pip install -e .[dev]

# Проверьте установку
ru-fin-helper --help
```

### Установка на RHEL / Rocky / Alma

```bash
# Установите Python 3.10+ (может потребоваться EPEL или модуль AppStream)
sudo dnf install -y python3.10 python3-pip git

# Клонируйте репозиторий
git clone https://github.com/ranas-mukminov/ru-fin-monitoring-helper.git
cd ru-fin-monitoring-helper

# Создайте виртуальное окружение
python3.10 -m venv venv
source venv/bin/activate

# Установите пакет
pip install -e .[dev]

# Проверьте
ru-fin-helper --help
```

### Установка с Docker (примеры ELK/Loki)

```bash
# Установите Docker и Docker Compose (пример для Ubuntu)
sudo apt install -y docker.io docker-compose
sudo systemctl enable --now docker
sudo usermod -aG docker $USER
newgrp docker

# Запустите ELK-стек
cd examples/elk
docker compose -f docker-compose.elk-example.yml up -d

# Или запустите Loki-стек
cd examples/loki
docker compose -f docker-compose.loki-example.yml up -d
```

## Настройка

### Формат схемы событий

Схемы событий определяются в YAML в каталоге `schemas/events/`. Пример структуры:

```yaml
event_name: transaction_core
description_ru: "Основные этапы платежной транзакции"
category: transaction
criticality: high
use_cases:
  - aml
  - fraud_detection
  - audit
fields:
  - name: event_type
    type: keyword
    required: true
    description: "Идентификатор типа события"
  - name: ts
    type: datetime
    required: true
    description: "Временная метка события"
  - name: transaction_id
    type: keyword
    required: true
    description: "Уникальный ID транзакции"
  - name: amount
    type: float
    required: true
    description: "Сумма транзакции"
  - name: currency
    type: keyword
    required: true
    description: "Код валюты (RUB, USD и т.д.)"
```

### Формат профиля продукта

Определите характеристики вашего продукта в `examples/products/`:

```yaml
product_type: psp
channels:
  - web
  - api
geography: RU
features:
  - cards
  - merchants
description: "Пример российского платёжного шлюза (PSP)."
```

### Конфигурация пайплайна

Пример пайплайна ELK Logstash (`pipelines/elk/logstash_finmon_pipeline.conf`):

```ruby
input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
  }
  
  if [event_type] == "transaction_core" {
    mutate {
      add_tag => ["finmon", "transaction"]
    }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "finmon-%{+YYYY.MM.dd}"
  }
}
```

### Переменные окружения

Для базового использования CLI переменные окружения не требуются. Для продакшн-интеграций:

- `OPENSANCTIONS_API_KEY` — API-ключ OpenSanctions (опционально, для проверки санкций)
- `SANCTIONS_NETWORK_TOKEN` — Токен Sanctions Network (опционально)
- `ELK_HOST` — Endpoint Elasticsearch для кастомных развёртываний
- `GRAFANA_URL` — URL экземпляра Grafana

## Использование и типовые задачи

### Валидация схем событий

```bash
# Валидация одной схемы
ru-fin-helper validate-schema --file schemas/events/auth_login.yaml

# Валидация всех схем в каталоге
for schema in schemas/events/*.yaml; do
  ru-fin-helper validate-schema --file "$schema"
done
```

### Генерация заглушек пайплайнов

```bash
# Генерация пайплайна Logstash из схемы
ru-fin-helper generate-pipeline-stub \
  --schema schemas/events/transaction_core.yaml \
  --target elk \
  --out /tmp/transaction_pipeline.conf

# Генерация пайплайна Loki
ru-fin-helper generate-pipeline-stub \
  --schema schemas/events/kyc_lifecycle.yaml \
  --target loki \
  --out /tmp/kyc_loki_config.yaml
```

### AI-предложения событий

```bash
# Предложить минимальный набор событий для продукта
ru-fin-helper suggest-events \
  --product examples/products/simple_psp_ru.yaml \
  --out /tmp/suggested_events.yaml

# Просмотр предложенных событий
cat /tmp/suggested_events.yaml
```

### Генерация комплаенс-плейбуков

```bash
# Генерация плейбуков для профиля продукта
ru-fin-helper generate-playbooks \
  --product examples/products/simple_psp_ru.yaml \
  --events schemas/events/ \
  --out /tmp/playbooks/ \
  --risk-level high
```

### Доступ к веб-интерфейсам

После запуска Docker Compose стеков:

- **Kibana (ELK)**: http://localhost:5601
- **Grafana (Loki)**: http://localhost:3000 (учётные данные по умолчанию: admin/admin)
- **API Elasticsearch**: http://localhost:9200

### Импорт дашбордов Grafana

```bash
# Скопируйте JSON дашборда в контейнер Grafana
docker cp dashboards/grafana/finmon_overview.json \
  <grafana_container_id>:/tmp/

# Или импортируйте через UI Grafana:
# Settings → Data Sources → Add Elasticsearch
# Dashboards → Import → Upload JSON
```

## Обновление

### Обновление из Git

```bash
# Перейдите в репозиторий
cd ru-fin-monitoring-helper

# Получите последние изменения
git pull origin main

# Переустановите пакет
pip install -e .[dev]

# Запустите тесты для проверки
pytest tests/unit
```

### Обновление Docker-образов

```bash
# Получите последние образы
docker compose -f examples/elk/docker-compose.elk-example.yml pull

# Пересоздайте контейнеры
docker compose -f examples/elk/docker-compose.elk-example.yml up -d --force-recreate
```

### Миграция схем

При обновлении схем проверьте совместимость:

```bash
# Валидация обновлённой схемы
ru-fin-helper validate-schema --file schemas/events/transaction_core.yaml

# Регенерация пайплайнов при изменении схемы
ru-fin-helper generate-pipeline-stub \
  --schema schemas/events/transaction_core.yaml \
  --target elk \
  --out pipelines/elk/transaction_pipeline.conf
```

## Логи и устранение неполадок

### Просмотр логов

```bash
# Логи Docker Compose
docker compose -f examples/elk/docker-compose.elk-example.yml logs -f

# Логи конкретного сервиса
docker compose logs elasticsearch
docker compose logs logstash
docker compose logs kibana

# Логи CLI (с повышенной детализацией)
ru-fin-helper --verbose validate-schema --file schemas/events/auth_login.yaml
```

### Типичные проблемы

**Порт уже используется**

```bash
# Проверьте, что использует порт 9200 (Elasticsearch)
sudo lsof -i :9200

# Завершите процесс или измените порт в docker-compose.yml
```

**Elasticsearch не запускается (недостаточно памяти)**

```bash
# Увеличьте vm.max_map_count (ограничение ядра Linux)
sudo sysctl -w vm.max_map_count=262144

# Сделайте постоянным
echo "vm.max_map_count=262144" | sudo tee -a /etc/sysctl.conf
```

**Нет данных в дашбордах Grafana**

- Проверьте правильность настройки источника данных Elasticsearch
- Убедитесь, что шаблон индекса соответствует `finmon-*`
- Проверьте, что логи поступают через Logstash (проверьте логи Logstash)
- Протестируйте запрос Elasticsearch: `curl http://localhost:9200/finmon-*/_count`

**Валидация схемы не проходит**

```bash
# Проверьте синтаксис YAML
yamllint schemas/events/transaction_core.yaml

# Просмотрите сообщения об ошибках для отсутствующих обязательных полей
ru-fin-helper validate-schema --file schemas/events/transaction_core.yaml
```

**Отказано в доступе (Docker)**

```bash
# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker
```

## Замечания по безопасности

### Чеклист продакшн-развёртывания

- **Измените пароли по умолчанию** — Elasticsearch, Kibana, Grafana поставляются с дефолтными учётными данными. Немедленно измените.
- **Включите TLS/SSL** — Настройте HTTPS для Elasticsearch, Kibana, Grafana в продакшене.
- **Ограничьте сетевой доступ** — Используйте правила фаервола (iptables/ufw) для ограничения доступа к портам мониторинга (9200, 5601, 3000).
- **Доступ через VPN/bastion** — Не выставляйте Kibana/Grafana напрямую в Интернет; используйте VPN или SSH-туннелирование.
- **Ротация API-ключей** — Если используете API санкций, регулярно обновляйте ключи и храните в secrets manager.
- **Политики хранения данных** — Настройте управление жизненным циклом индексов в Elasticsearch, чтобы избежать неограниченного роста хранилища.
- **Санитизация ПДн** — Маскируйте или хэшируйте чувствительные поля (ID пользователей, суммы) в логах перед индексацией.
- **Аудит-логирование** — Включите логи аудита для доступа к Elasticsearch и Grafana.

### Соображения о соответствии нормам

- Этот инструментарий предоставляет только техническую инфраструктуру; консультируйтесь с юридическими и комплаенс-командами по регуляторным требованиям.
- Не коммитьте реальные API-ключи, продакшн-конфиги или персональные данные в систему контроля версий.
- Функционал проверки санкций является заглушкой; реальное развёртывание требует лицензированных источников данных и юридической экспертизы.

## Структура проекта

```
ru-fin-monitoring-helper/
├─ cli/                         # CLI-приложение
│  ├─ commands/                 # Реализации команд
│  │  ├─ validate_schema.py
│  │  ├─ generate_pipeline_stub.py
│  │  ├─ suggest_events.py
│  │  └─ generate_playbooks.py
│  └─ main.py                   # Точка входа CLI
├─ schemas/                     # Определения схем событий
│  ├─ events/                   # Основные схемы событий (YAML)
│  │  ├─ auth_login.yaml
│  │  ├─ kyc_lifecycle.yaml
│  │  ├─ transaction_core.yaml
│  │  ├─ sanctions_screening.yaml
│  │  └─ ...
│  ├─ pipelines/                # Определения схем пайплайнов
│  └─ reference/                # Схемы справочных данных
├─ pipelines/                   # Шаблоны конфигураций пайплайнов
│  ├─ elk/                      # Конфигурации Logstash
│  └─ loki/                     # Конфигурации Loki
├─ dashboards/                  # Определения дашбордов Grafana
│  └─ grafana/                  # JSON-экспорты дашбордов
├─ ai/                          # Модули AI-помощников
│  └─ src/ru_fin_ai_helper/     # Генератор событий, генератор плейбуков
├─ sanctions/                   # Заглушки санкционных клиентов
│  ├─ opensanctions_client/     # Интеграция OpenSanctions
│  └─ sanctions_network_client/ # Интеграция Sanctions Network
├─ examples/                    # Примеры использования
│  ├─ products/                 # Примеры профилей продуктов
│  ├─ logs/                     # Примеры лог-файлов
│  ├─ playbooks/                # Примеры плейбуков
│  ├─ elk/                      # Настройка Docker Compose для ELK
│  └─ loki/                     # Настройка Docker Compose для Loki
├─ tests/                       # Набор тестов
│  ├─ unit/                     # Юнит-тесты
│  └─ integration/              # Интеграционные тесты
├─ scripts/                     # Скрипты разработки
│  ├─ lint.sh                   # Линтинг
│  ├─ security_scan.sh          # Проверки безопасности
│  └─ dev_run_all_tests.sh      # Полный набор тестов
├─ pyproject.toml               # Метаданные пакета
├─ LEGAL.md                     # Юридический дисклеймер
├─ CONTRIBUTING.md              # Рекомендации по внесению вклада
└─ LICENSE                      # Лицензия Apache-2.0
```

## Планы развития

- [ ] Добавить больше схем событий (вывод криптовалюты, онбординг мерчантов, цифровые отпечатки устройств)
- [ ] Prometheus-экспортер для метрик FinMon
- [ ] Примеры пайплайнов Vector.dev
- [ ] Интеграция с агрегацией логов российских облачных провайдеров (Yandex Cloud Logging, VK Cloud)
- [ ] Расширенный генератор AI-плейбуков с оценкой рисков
- [ ] Демо проверки санкций в реальном времени (с тестовыми данными)
- [ ] Шаблоны правил алертинга для Prometheus/Grafana

## Как внести вклад

Вклады приветствуются! Пожалуйста, следуйте подходу **сначала тесты**:

1. **Сначала напишите тесты** — Добавьте или обновите юнит-тесты для валидаторов, CLI-команд, AI-помощников или санкционных клиентов.
2. **Реализуйте код** — Напишите код, чтобы тесты прошли.
3. **Запустите все тесты** — Перед коммитом выполните:

   ```bash
   ./scripts/dev_run_all_tests.sh
   ```

   Это запускает линтеры (ruff, mypy, yamllint) и pytest.

4. **Откройте Pull Request** — Отправьте PR с чётким описанием изменений.

### Стиль кода

- Следуйте PEP 8 и PEP 257
- Используйте type hints (совместимые с mypy)
- Делайте функции небольшими и сфокусированными
- Пишите описательные сообщения коммитов

### Юридические границы

- Не коммитьте реальные API-ключи, продакшн-конфиги или персональные данные
- Соблюдайте лицензионные и регуляторные ограничения
- Используйте английские или русские комментарии там, где код сложен

## Лицензия

Этот проект лицензирован под **Apache License 2.0**. См. полный текст в [LICENSE](LICENSE).

## Автор и коммерческая поддержка

**Автор**: [Ранас Мукминов](https://github.com/ranas-mukminov)

Этот инструментарий разрабатывается и поддерживается инженером DevOps/DevSecOps, специализирующимся на инфраструктуре observability и compliance для финтех- и платёжных платформ.

### Профессиональные услуги

Для внедрения промышленного уровня, разработки кастомных схем и постоянной поддержки посетите **[run-as-daemon.ru](https://run-as-daemon.ru)** или свяжитесь с автором через GitHub.

Услуги включают:

- **Настройку observability для финмониторинга** — Сквозное логирование, дашборды и алертинг для AML/фрод-детекции
- **Кастомизацию пайплайнов** — Индивидуальные конфигурации ELK/Loki/Vector для вашей инфраструктуры
- **Аудит инфраструктуры compliance** — Проверка существующих систем на соответствие регуляторным требованиям
- **Интеграцию с российскими облачными платформами** — Yandex Cloud, VK Cloud и другие провайдеры
- **Обучение и передачу знаний** — Воркшопы для команд DevSecOps

---

**Дисклеймер**: Этот проект является техническим инструментарием и не является юридической консультацией. Он не заменяет официальные регуляторные требования или комплаенс-рекомендации. Ответственность за соблюдение норм лежит на конечном пользователе. Продакшн API-ключи, реальные санкционные списки или персональные данные не включены в этот репозиторий.
